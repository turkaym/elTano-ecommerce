# Tasks: Baseline Test Debt Cleanup

## Phase 1: RED — Confirm and Isolate Current Failures

- [x] 1.1 Run backend target test to capture current security-assertion failure: `./gradlew :backend:test --tests "com.eltano.ecommerce.orders.api.OrderDraftControllerTest.adminEndpointsRemainProtectedInMvpScope"`.
- [x] 1.2 Run backend target test to capture MVC context failure from missing audit dependency: `./gradlew :backend:test --tests "com.eltano.ecommerce.orders.api.MercadoPagoWebhookControllerTest"`.
- [x] 1.3 Run frontend target test to capture flags module-shape failure: `pnpm --dir frontend vitest run src/app/App.payment.test.tsx`.

## Phase 2: GREEN — Minimal Test-Only Fixes in Dependency Order

- [x] 2.1 In `backend/src/test/java/com/eltano/ecommerce/orders/api/OrderDraftControllerTest.java`, update `adminEndpointsRemainProtectedInMvpScope` expected status to the current CSRF/auth precedence and add a comment that this is contract alignment only.
- [x] 2.2 Validate 2.1 passes: `./gradlew :backend:test --tests "com.eltano.ecommerce.orders.api.OrderDraftControllerTest.adminEndpointsRemainProtectedInMvpScope"`.
- [x] 2.3 In `backend/src/test/java/com/eltano/ecommerce/orders/api/MercadoPagoWebhookControllerTest.java`, add `@MockBean AdminAuditService` (or equivalent required mock) while keeping current `SecurityConfig` import path intact.
- [x] 2.4 Validate 2.3 passes and context boots: `./gradlew :backend:test --tests "com.eltano.ecommerce.orders.api.MercadoPagoWebhookControllerTest"`.
- [x] 2.5 In `frontend/src/app/App.payment.test.tsx`, replace full `../shared/config/flags` mock with partial mock (`importActual` + overrides) that preserves exports and explicitly sets flags needed for payment flow.
- [x] 2.6 Validate 2.5 passes without export-shape errors: `pnpm --dir frontend vitest run src/app/App.payment.test.tsx`.

## Phase 3: REFACTOR + Verification

- [x] 3.1 Refactor duplicated mock/comment text for clarity only (no behavior change) across the three edited test files.
- [x] 3.2 Run focused regression suite: `./gradlew :backend:test --tests "com.eltano.ecommerce.orders.api.OrderDraftControllerTest" --tests "com.eltano.ecommerce.orders.api.MercadoPagoWebhookControllerTest"` and `pnpm --dir frontend vitest run src/app/App.payment.test.tsx`.
- [x] 3.3 Verify scope boundary: ensure diff includes only test files: `git diff --name-only -- backend/src/test frontend/src/app/App.payment.test.tsx`.

## Result Contract

- All three targeted test commands pass.
- No module export/mocking runtime errors in `App.payment.test.tsx`.
- MVC tests load with `SecurityConfig` and audit dependencies satisfied via test mocks.
- Only test files are changed; no production runtime/security/audit behavior is modified.
