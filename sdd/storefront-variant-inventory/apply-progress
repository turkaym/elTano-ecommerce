# Apply Progress — storefront-variant-inventory (Batch 1)

## Mode
Standard (non-strict TDD)

## Completed (Batch 1)
- [x] **1.1** Add `product_type`, `inventory_policy`, `stock_base_grams` migration defaults/checks/indexes.
- [x] **1.2** Add domain enums/fields and legacy fallback mapping (`null -> PER_VARIANT/ENVASADO`).
- [x] **1.3** Update backend contracts for public/admin metadata while preserving existing checkout payload shape.

## Notes
- `ProductRepository` query structure was already compatible (entity-loading of `Product` includes new product-level fields), so no query shape change was needed for Batch 1.
- Batch 1 intentionally does **not** rewrite stock decrement behavior (deferred to Batch 2).

## Validation
- Backend compile: `./mvnw -q -DskipTests compile` ✅
- Backend targeted tests: `./mvnw -q -Dtest=OrderDraftServiceTest test` ✅, `./mvnw -q -Dtest=OrderDraftControllerTest test` ✅
- Backend full tests: `./mvnw -q test` ✅
- Frontend tests: `npm test` ✅
- Frontend build: `npm run build` ✅

## Remaining
- Batch 2 tasks (2.1, 2.2, 2.3)
- Batch 3 tasks (3.1, 3.2, 3.3, 3.4)
- Batch 4 tasks (4.1, 4.2, 4.3)
