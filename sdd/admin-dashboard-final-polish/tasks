# Tasks: Admin Dashboard Final Polish

## Batch 1 — Admin access authority foundation (strict TDD)
- [x] 1.1 **RED**: Update `frontend/src/features/admin/auth/AdminGuard.test.tsx` with failing cases for bootstrap outcomes on direct `/admin/*` load (2xx => allow, 401 => login-entry UX, 403 => forbidden UX).
- [x] 1.2 **GREEN**: Modify `frontend/src/features/admin/auth/adminAccess.ts` to expose backend-probe bootstrap + explicit `AdminAccessState` mapping; remove `sessionStorage` as authority.
- [x] 1.3 **GREEN**: Modify `frontend/src/features/admin/auth/AdminGuard.tsx` to consume async bootstrap state and route deterministically for unauthenticated vs unauthorized users.
- [x] 1.4 **REFACTOR**: Centralize shared guard state helpers/messages in `adminAccess.ts` and simplify guard branching without changing behavior.
- [x] 1.5 **Validate**: `npm run test -- src/features/admin/auth/AdminGuard.test.tsx`

## Batch 2 — Admin shell and nested route wiring (strict TDD)
- [x] 2.1 **RED**: Add/extend route integration tests in `frontend/src/features/admin/auth/AdminGuard.test.tsx` (or adjacent route test) for `/admin` landing, nested workflow navigation, and refresh persistence.
- [x] 2.2 **GREEN**: Modify `frontend/src/app/routes/AppRoutes.tsx` to add nested `/admin` child routes for products, categories, orders, and catalog jobs, including default admin redirect.
- [x] 2.3 **GREEN**: Modify `frontend/src/features/admin/layout/AdminShell.tsx` to render operational nav + `Outlet`, plus explicit login/logout/forbidden UX states.
- [x] 2.4 **REFACTOR**: Extract admin nav config/constants in `AdminShell.tsx` to keep route labels/paths consistent and maintainable.
- [x] 2.5 **Validate**: `npm run test -- src/features/admin/auth/AdminGuard.test.tsx`

## Batch 3 — Workflow pages + service DTO contracts (strict TDD)
- [x] 3.1 **RED**: Expand `frontend/src/features/admin/services/adminOperationsService.test.tsx` with failing DTO-shape assertions (orders `items`, products/categories lists, jobs list/detail, rows/report).
- [x] 3.2 **GREEN**: Modify `frontend/src/features/admin/services/adminOperationsService.ts` with strict interfaces and typed helpers for products, categories, orders, catalog jobs, rows, and report.
- [x] 3.3 **GREEN**: Create `frontend/src/features/admin/pages/AdminProductsPage.tsx` and `AdminCategoriesPage.tsx` with actionable list/empty states and primary next actions.
- [x] 3.4 **GREEN**: Create `frontend/src/features/admin/pages/AdminOrdersPage.tsx` with paged order list + status filtering from typed service contract.
- [x] 3.5 **GREEN**: Create `frontend/src/features/admin/pages/AdminCatalogJobsPage.tsx` with job diagnostics (status/lastError/rows/report) and non-placeholder operational details.
- [x] 3.6 **REFACTOR**: Reuse shared admin page UI primitives (loading/empty/error blocks) across new page files to reduce duplication.
- [x] 3.7 **Validate**: `npm run test -- src/features/admin/services/adminOperationsService.test.tsx`

## Batch 4 — Catalog cancel closure + final verification (strict TDD)
- [x] 4.1 **RED**: Add failing tests in `adminOperationsService.test.tsx` and `AdminCatalogJobsPage` test coverage for cancel being non-actionable when backend contract is unsupported.
- [x] 4.2 **GREEN**: Ensure UI never exposes active cancel action; render disabled/hidden cancel with clear “unsupported” status text in `AdminCatalogJobsPage.tsx`.
- [x] 4.3 **GREEN (optional backend)**: If required by tests/contract clarity, adjust `backend/src/main/java/com/eltano/ecommerce/catalog/jobs/api/AdminCatalogJobController.java` to keep explicit `501` semantics documented (no new API).
- [x] 4.4 **REFACTOR**: Normalize diagnostics presentation for row/report failures so operator triage context is concise and consistent.
- [x] 4.5 **Validate**: `npm run test && npm run lint && npm run build`

## Global completion gate
- [x] G1 Verify scope boundary: only products/categories/orders/catalog jobs touched; no new admin domains/routes introduced.
- [x] G2 Verify logout transition clears stale client access marker and blocks admin routes until re-authenticated.
- [x] G3 Optional backend check (only if backend file changed): `./mvnw.cmd -q -DskipTests compile`
