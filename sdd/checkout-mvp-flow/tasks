# Tasks: Checkout MVP Flow

## Phase 1: Infrastructure / Foundation
- [x] 1.1 Create frontend checkout/cart contracts in `frontend/src/shared/types/checkout.ts` and align names/types with backend DTOs (`customerName`, `phone`, `note`, `items[]`, `draftId`, `reference`, totals).
- [x] 1.2 Implement cart persistence utilities in `frontend/src/features/cart/storage/cartStorage.ts` using key `eltano.cart.v1`, including safe parse + corrupt-payload fallback signal.
- [x] 1.3 Scaffold cart state store in `frontend/src/features/cart/state/cartStore.ts` with actions (`addItem`, `setQty`, `removeItem`, `clear`) and derived totals.
- [x] 1.4 Add backend order-draft skeleton: `OrderDraft`, `OrderDraftLine`, status enum `DRAFT`, repositories, and additive DB migration/table mapping under `backend/src/main/java/com/eltano/ecommerce/orders/**`.
- [x] 1.5 Define API boundary in backend DTO/controller skeleton: `CreateOrderDraftRequest/Response` and `POST /api/orders/drafts` in `orders/api`, plus `SecurityConfig` permit rule for this public endpoint.

## Phase 2: Implementation / Feature Delivery
- [x] 2.1 Implement mock-aligned storefront shell in `frontend/src/app/App.tsx` + styles (category nav, hero split, icons, CTA hierarchy), preserving approved visual direction.
- [x] 2.2 Implement visual-only search component in `frontend/src/features/search/components/SearchBar.tsx` with no filtering, navigation, or API side effects.
- [x] 2.3 Wire catalog product cards (`frontend/src/features/catalog/components/FeaturedProductsSection.tsx`) to cart actions, including out-of-stock disabled states.
- [x] 2.4 Build cart UI module (panel/list/qty controls/remove) consuming `cartStore`, hydrating from storage on load, and showing recoverable warning on invalid stored JSON.
- [x] 2.5 Build checkout form in `frontend/src/features/checkout/components/CheckoutForm.tsx` with required field validation + inline errors; block submit on invalid/missing data or empty cart.
- [x] 2.6 Implement `OrderDraftService` transactional logic in backend: validate variants/stock, reserve stock atomically, calculate totals, generate draft reference, persist draft + lines.
- [x] 2.7 Complete controller + exception mapping for `POST /api/orders/drafts` returning `201` with `whatsappMessage`; ensure 400/409/5xx behaviors match `RestExceptionHandler` shape.
- [x] 2.8 Add frontend API integration in `frontend/src/features/checkout/services/checkoutService.ts` and `shared/api/httpClient.ts` (`postJson`); on success open `wa.me` with encoded message, on failure keep user on checkout.

## Phase 3: Testing / Verification
- [x] 3.1 RED: Add failing JUnit tests for `OrderDraftService` (success reserve path, insufficient stock conflict, missing variant validation, totals/reference generation).
- [x] 3.2 GREEN: Implement/fix service code until `OrderDraftService` tests pass; REFACTOR: clean naming/duplication while keeping tests green.
- [x] 3.3 Add WebMvc integration tests for `POST /api/orders/drafts` contracts: 201 success payload, 400 field errors, 409 stock conflict.
- [x] 3.4 Run frontend static verification (`npm run lint`, `npm run build`) and backend verification (`mvn test`), fixing regressions in changed modules. Full backend suite now passes with isolated test profile for context bootstrap.
- [x] 3.5 Verify MVP storefront behaviors for strict-TDD evidence: shell section rendering/responsive visibility, visual-only search no side effects, cart reducer CRUD + localStorage persistence/recovery, and checkout redirect gating (no redirect on API failure; redirect on success with wa.me payload). Evidence now automated in Vitest + RTL test suites.
- [x] 3.6 Perform FE/BE contract alignment check using real request/response samples to confirm DTO/type parity and no payload drift before handoff.

## Status
- Completed: **19/19**
- Remaining: **0**
