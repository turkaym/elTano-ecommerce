# Archive Report

**Change**: `checkout-payment-mercadopago`  
**Project**: `elTano_ecommerce`  
**Archived On**: `2026-04-17`  
**Archive Mode**: `engram` (with local artifact mirror in `sdd/`)

---

## Traceability Sources

### Required SDD Artifacts (Engram lookup)
- `sdd/checkout-payment-mercadopago/proposal` → **Not found**
- `sdd/checkout-payment-mercadopago/spec` → **Not found**
- `sdd/checkout-payment-mercadopago/design` → **Not found**
- `sdd/checkout-payment-mercadopago/tasks` → **Not found**
- `sdd/checkout-payment-mercadopago/verify-report` → **Observation #476** (latest revision, authoritative verify topic)

### Supplemental Closure Evidence
- **Observation #484** (`verification/mp-e2e-sandbox-success`): end-to-end sandbox success with automatic webhook callbacks and canonical `PAID` status.
- **Observation #485** (`security/mercadopago-webhook-hardening`): production hardening decision for webhook signature strictness and credential rotation.
- Local filesystem artifact: `sdd/checkout-payment-mercadopago/verify-report` (legacy/stale compared with latest Engram verify observation).

---

## Closure Validation Context (Included)

Validated closure evidence for this archive includes:
- End-to-end sandbox flow validated.
- Automatic webhook callbacks observed with HTTP `200` in ngrok.
- Backend canonical status endpoint returns `PAID`.
- Database reflects `PAID` state transition and webhook event persistence.
- Local debugging used optional `MP_WEBHOOK_SIGNATURE_REQUIRED=false`; production recommendation remains `true` plus credential rotation.

---

## Archive Decision

**Closure Status**: ✅ **CLOSED**

Rationale:
1. Authoritative verification evidence (`#476`) indicates final MVP paid path is validated.
2. Runtime sandbox evidence (`#484`) confirms automatic callback and state convergence to canonical `PAID`.
3. Security posture and operational constraints are documented (`#485`) for production readiness.

Note:
- No OpenSpec filesystem merge/move was executed (project does not use `openspec/` artifacts for this change).
- This archive report is the formal closeout artifact for SDD archive traceability.

---

## Carry-over Operational Notes

1. **Production webhook strictness**: enforce `MP_WEBHOOK_SIGNATURE_REQUIRED=true` in non-test environments.
2. **Secret hygiene**: require non-default `MP_WEBHOOK_SECRET`; treat blank/default secret as deployment blocker.
3. **Credential rotation**: rotate `MP_ACCESS_TOKEN` and `MP_WEBHOOK_SECRET` after sandbox/debug cycles.
4. **Debug flag scope**: `MP_WEBHOOK_SIGNATURE_REQUIRED=false` is allowed only for controlled local debugging; never keep it disabled in shared/staging/production.

---

## SDD Archive Outcome

`checkout-payment-mercadopago` is archived as completed from an SDD lifecycle perspective, with security hardening actions explicitly carried into operations.
