## Apply Progress — catalog-navigation-and-search

**Change**: `catalog-navigation-and-search`  
**Mode**: Strict TDD  
**Purpose of this update**: restore explicit auditable TDD cycle traceability for completed execution batches.

---

## TDD Cycle Evidence

| Task/Batch | Safety Net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|
| **Batch A (T1.1–T1.4): Router foundation + navbar routing** | Existing checkout/home behaviors already covered by app/payment tests before route-shell refactor. | Added/updated route+navbar integration assertions in `frontend/src/app/App.test.tsx` for `/`, `/categorias`, `/categorias/:slug`, `/productos`, `/checkout/return`; initial failures validated missing/new wiring needs. | Implemented BrowserRouter wiring + route shell + nav links (`main.tsx`, `AppRoutes.tsx`, `App.tsx`, `StorefrontNav.tsx`) until route tests passed. | Validated both navigation modes (navbar click-through and direct URL entry), preventing single-path false confidence. | Kept routing composition declarative and isolated in route module; adjusted tests to assert user-visible route behavior (not internals). |
| **Batch B (T2.1–T2.4): Shared catalog query layer** | Pre-existing catalog pages and app shell tests remained green while query layer was introduced. | Authored unit tests for pure query utility + fallback hook (`catalogQuery.test.ts`, `useCatalogQuery.test.ts`) first to expose unimplemented/edge behaviors. | Implemented shared contracts, filtering/sort pipeline, and fallback-capable query hook until all new unit tests passed. | Added multiple matching/filter paths (name/description/category, stock, slug resolution, fallback path) to avoid overfitting to a single case. | Consolidated filtering logic into pure utility + shared hook to remove duplication and keep page components thin. |
| **Batch C (T3.1–T3.4): Categories index/detail experience** | Batch B unit coverage acted as regression guard for category derivation/filter primitives reused by pages. | Added category route tests (`CategoriesPage.test.tsx`, `CategoryDetailPage.test.tsx`) for list, click-through, valid slug, and invalid-slug deterministic empty state; early failures captured missing UI/route behavior. | Implemented reusable grid + category index/detail pages and route linkage until category tests passed. | Covered both valid and invalid slug trajectories and full click-through flow from index to detail route. | Reused `CatalogProductGrid` and shared query layer; removed page-specific duplication in favor of shared rendering/filter primitives. |
| **Batch D (T4.1–T4.4): Products search/filters + URL state** | Existing category/route suites remained green to ensure products-page work did not regress prior navigation slices. | Added products/app integration tests for URL-driven query params, combined filters/sort, category-text matching, and empty-results state (`ProductsPage.test.tsx`, `App.test.tsx`). | Implemented controlled SearchBar contract + `/productos` query-param source-of-truth behavior (`q`, `category`, `stock`, `sort`) with normalized defaults until tests passed. | Exercised multi-axis combinations (search + category + stock + sort) and navigation persistence (reload/back-forward) to validate generalized behavior. | Centralized URL-state normalization and kept page logic aligned with shared query pipeline; eliminated ad-hoc state drift between controls and URL. |
| **Batch E (T5.1–T5.3): Checkout return non-regression gate** | Existing payment regression suite provided baseline protection for checkout return contract before router hardening. | Extended `frontend/src/app/App.payment.test.tsx` with direct `/checkout/return?draftId=...` non-PAID retry-path assertions and added cross-regression check in `App.test.tsx`; initial assertion mismatch exposed copy/flag expectations. | Hardened explicit `/checkout/return` route behavior and aligned assertions with real fallback semantics; lint/test/build all passed after patching. | Validated canonical PAID path + non-PAID retry path + post-return catalog navigation continuity, ensuring behavior across divergent return states. | Preserved explicit route declaration and behavior-focused tests to prevent future route interception regressions by catalog paths. |
| **Final accent patch (post-batch fix): `Categorias` → `Categorías`** | Full app navigation + payment regression tests already present to protect against unintended copy-side regressions. | Updated navbar/accessibility-name assertions expecting accented label (`Categorías`) and observed failure against previous unaccented text. | Patched `StorefrontNav.tsx` label and aligned `App.test.tsx` assertions; frontend tests/lint returned green. | Verified accent consistency in both rendered nav label and route-navigation assertions (UI + integration perspective). | Limited change to copy + assertions only; no behavioral/router logic churn introduced in final patch. |

---

## Status
Strict-TDD cycle evidence is now explicitly documented for **all completed batches (A–E) plus the final accent patch**.
