# Tasks: Project Release Readiness

## Phase 1: Test Harness Foundation (TDD RED)

- [x] 1.1 In `frontend/src/app/routes/AppRoutes.admin-routes.test.tsx`, replace ambient `globalThis.fetch` assumptions with RED tests that spy `bootstrapAdminSession`/`listAdminProducts`, assert loading→settled boundaries, and initially fail under current setup.
- [x] 1.2 In the same file, add explicit mock setup/teardown per test case to prevent hidden state reuse; keep assertions scoped to admin route behavior only (no production logic edits).
- [x] 1.3 Validation (expect RED): `npx vitest run frontend/src/app/routes/AppRoutes.admin-routes.test.tsx --coverage`.

## Phase 2: Determinism Implementation (TDD GREEN)

- [x] 2.1 Update `frontend/src/app/routes/AppRoutes.admin-routes.test.tsx` to GREEN by implementing seam-level resolved mocks (`authenticated`, product list) and deterministic waits before asserting “Sin productos”.
- [x] 2.2 Update `frontend/src/test/setupTests.ts` shared `afterEach` to call `vi.restoreAllMocks()` so mock/spies cannot leak across test boundaries.
- [x] 2.3 Validation (single pass): `npx vitest run frontend/src/app/routes/AppRoutes.admin-routes.test.tsx --coverage` must pass.
- [x] 2.4 Validation (strict stability): run 3 consecutive coverage passes and require all green:
  1) `npx vitest run frontend/src/app/routes/AppRoutes.admin-routes.test.tsx --coverage`
  2) `npx vitest run frontend/src/app/routes/AppRoutes.admin-routes.test.tsx --coverage`
  3) `npx vitest run frontend/src/app/routes/AppRoutes.admin-routes.test.tsx --coverage`.

## Phase 3: Release Gate Artifacts (TDD REFACTOR + Governance)

- [x] 3.1 Create `docs/release-readiness-checklist.md` with binary blocking gates for `npx vitest run --coverage`, `npm test -- --watch=false`, `npm run lint`, `npm run build`, and backend verify (`./mvnw.cmd -B clean verify` Windows / `./mvnw -B clean verify` Unix; `mvn -B clean verify` optional); include evidence columns (executor, timestamp, exit code, log/artifact link).
- [x] 3.2 Add warning-debt acceptance section in `docs/release-readiness-checklist.md` requiring owner + follow-up issue per unresolved WARNING, and a final READY/NOT READY decision block.
- [x] 3.3 Update `README.md` verification/release section to link `docs/release-readiness-checklist.md` as the required approval artifact.

## Phase 4: Full Release Validation

- [x] 4.1 Run and record frontend quality gates: `npx vitest run --coverage`, `npm test -- --watch=false`, `npm run lint`, `npm run build`.
- [x] 4.2 Run and record backend gate per wrapper-first policy (`./mvnw.cmd -B clean verify` on Windows / `./mvnw -B clean verify` on Unix).
- [x] 4.3 Confirm scope guard by reviewing touched files are test/docs only (`frontend/src/app/routes/AppRoutes.admin-routes.test.tsx`, `frontend/src/test/setupTests.ts`, `README.md`, `docs/release-readiness-checklist.md`) with no production behavior changes.

## Phase 5: Remediation — Backend Gate Policy Alignment

- [x] 5.1 Document canonical backend blocking gate as wrapper-first cross-platform policy: `./mvnw.cmd -B clean verify` (Windows) / `./mvnw -B clean verify` (Unix), with `mvn -B clean verify` optional when installed.
- [x] 5.2 Update `README.md` and `docs/release-readiness-checklist.md` to reflect wrapper-first policy and release decision criteria.
- [x] 5.3 Re-run backend blocking gate using wrapper command and record evidence in release checklist.
