# Skill Registry

Generated by `sdd-init` on 2026-05-03 for project `eltano-ecommerce`.

## Resolution Rules

- Project-level skills override user-level skills with the same name.
- Ignored entries: `sdd-*`, `_shared`, `skill-registry`.
- User-level source priority when no project override exists: `.config/opencode` -> `.claude` -> `.cursor` -> `.copilot`.

## Registered Skills

| Skill | Source | Trigger | Description |
|---|---|---|---|
| `branch-pr` | `C:\Users\marco\.config\opencode\skills\branch-pr\SKILL.md` | When creating a pull request, opening a PR, or preparing changes for review. | PR creation workflow for Agent Teams Lite following the issue-first enforcement system. |
| `go-testing` | `C:\Users\marco\.config\opencode\skills\go-testing\SKILL.md` | When writing Go tests, using teatest, or adding test coverage. | Go testing patterns for Gentleman.Dots, including Bubbletea TUI testing. |
| `issue-creation` | `C:\Users\marco\.config\opencode\skills\issue-creation\SKILL.md` | When creating a GitHub issue, reporting a bug, or requesting a feature. | Issue creation workflow for Agent Teams Lite following the issue-first enforcement system. |
| `judgment-day` | `C:\Users\marco\.config\opencode\skills\judgment-day\SKILL.md` | When user says "judgment day", "judgment-day", "review adversarial", "dual review", "doble review", "juzgar", "que lo juzguen". | Parallel adversarial review protocol with dual blind judges and convergence rounds. |
| `skill-creator` | `C:\Users\marco\.config\opencode\skills\skill-creator\SKILL.md` | When user asks to create a new skill, add agent instructions, or document patterns for AI. | Creates new AI agent skills following the Agent Skills spec. |

## Project Conventions

- No project convention files detected in repository root (`agents.md`, `AGENTS.md`, `CLAUDE.md`, `.cursorrules`, `GEMINI.md`, `copilot-instructions.md`).

## Compact Rules

### branch-pr
- Every PR MUST link an approved issue.
- Every PR MUST have exactly one `type:*` label.
- PR body MUST include an issue-closing keyword (`Closes #N`, `Fixes #N`, or `Resolves #N`).

### go-testing
- Prefer table-driven tests for Go unit tests.
- For Bubbletea, test model state transitions and teatest interaction flows.
- Keep golden files stable and update only for intentional behavior changes.

### issue-creation
- Use issue templates only (blank issues disabled).
- New issues start as `status:needs-review`; PRs require `status:approved` first.
- Direct questions to GitHub Discussions instead of Issues.

### judgment-day
- Run two independent blind judges in parallel on the same scope.
- Treat findings as confirmed only when both judges agree.
- Re-judge after fixes and escalate if convergence fails after two rounds.

### skill-creator
- Include required frontmatter (`name`, `description`, `license`, `metadata`).
- Use canonical skill structure (`SKILL.md`, optional `assets/`, optional `references/`).
- Document explicit trigger phrases for reliable auto-resolution.
